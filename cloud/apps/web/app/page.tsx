/**
 * Cloud Web — extends the core web app with premium UI components.
 *
 * TODO: Import and extend core Next.js pages/components here.
 */

export default function CloudHome() {
  return <div>TaskFlow Cloud — Premium Features</div>;
}
