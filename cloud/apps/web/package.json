{
  "name": "cloud-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'cloud-web: no build yet'",
    "dev": "echo 'cloud-web: extend core web with premium UI'"
  },
  "dependencies": {
    "@todoist/shared": "workspace:*"
  }
}
