name: OpenFocus
options:
  bundleIdPrefix: dev.openfocus
  deploymentTarget:
    iOS: "26.0"
    macOS: "26.0"
  xcodeVersion: "26.0"
  developmentLanguage: en
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.0"
    SWIFT_STRICT_CONCURRENCY: complete
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"

packages:
  KeychainAccess:
    url: https://github.com/kishikawakatsumi/KeychainAccess
    from: 4.2.2

# Layered targets. The Xcode project compiles the engine sources directly into
# framework targets (it does NOT consume the root Package.swift products) so the
# GUI apps and the `openfocus` tool link the same layers. SwiftData is a system
# framework, auto-linked wherever `import SwiftData` appears.
targets:

  # Pure engine — value types, AI client seam, natural-language date parsing,
  # Keychain. No SwiftData. Shared by every other target. macOS + iOS.
  OpenFocusCore:
    type: framework
    supportedDestinations: [macOS, iOS]
    sources:
      - path: Sources/Core
    dependencies:
      - package: KeychainAccess
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.openfocus.core
        GENERATE_INFOPLIST_FILE: YES

  # Persistence + stateful services — SwiftData @Model types and ModelContext-bound
  # services (Task/Project/AI) + the CloudKit-ready ModelContainer. macOS + iOS.
  OpenFocusData:
    type: framework
    supportedDestinations: [macOS, iOS]
    sources:
      - path: Sources/Data
    dependencies:
      - target: OpenFocusCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.openfocus.data
        GENERATE_INFOPLIST_FILE: YES

  # CLI command parser + help/usage. Pure, no persistence. macOS only.
  OpenFocusCLIKit:
    type: framework
    platform: macOS
    sources:
      - path: Sources/CLIKit
    dependencies:
      - target: OpenFocusCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.openfocus.clikit
        GENERATE_INFOPLIST_FILE: YES

  OpenFocus-macOS:
    type: application
    platform: macOS
    deploymentTarget: "26.0"
    sources:
      - path: Sources/App
      - path: Sources/Features
      - path: Sources/SharedUI
      - path: Sources/Platform/macOS
      - path: Resources
    dependencies:
      - target: OpenFocusCore
      - target: OpenFocusData
    settings:
      base:
        # Deliberately identical to the iOS target's. One bundle id across both
        # platforms is what makes this one App Store product (Universal Purchase),
        # lets the two share an iCloud container, and lets Handoff pair them.
        # Don't re-add a .macos/.ios suffix — it splits the app in two.
        #
        # Safe to change now because nothing has shipped: no git tags, no GitHub
        # releases, and no App Store or TestFlight build ever went out under the old
        # dev.openfocus.app.macos / dev.openfocus.app.ios ids, so there is no install
        # base to migrate. After the first release this becomes a one-way door — a
        # SwiftData store lives in a directory keyed to the bundle id, so changing it
        # again would leave users' tasks behind unless the container is pointed at the
        # legacy store URL explicitly.
        PRODUCT_BUNDLE_IDENTIFIER: dev.openfocus.app
        PRODUCT_NAME: OpenFocus
        GENERATE_INFOPLIST_FILE: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.productivity
        INFOPLIST_KEY_NSHumanReadableCopyright: "© 2026 OpenFocus"
        # Hardened runtime enforces same-Team-ID library validation, which ad-hoc
        # (no-team) signing can't satisfy for the embedded frameworks. Re-enable it
        # for notarized distribution once a real DEVELOPMENT_TEAM is set.
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGN_STYLE: Automatic
    preBuildScripts:
      - name: SwiftLint
        basedOnDependencyAnalysis: false
        script: |
          if which swiftlint > /dev/null; then
            swiftlint
          else
            echo "warning: SwiftLint not installed"
          fi

  OpenFocus-iOS:
    type: application
    platform: iOS
    deploymentTarget: "26.0"
    sources:
      - path: Sources/App
      - path: Sources/Features
      - path: Sources/SharedUI
      - path: Sources/Platform/iOS
      - path: Resources
    dependencies:
      - target: OpenFocusCore
      - target: OpenFocusData
    settings:
      base:
        # Matches the macOS target on purpose; see the note there.
        PRODUCT_BUNDLE_IDENTIFIER: dev.openfocus.app
        PRODUCT_NAME: OpenFocus
        GENERATE_INFOPLIST_FILE: YES
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        INFOPLIST_KEY_CFBundleIconName: AppIcon
        INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight

  # `openfocus` CLI. Compiles Sources/CLI only (the @main entry + SwiftData-backed
  # executor) and links the layers it imports. macOS only.
  openfocus:
    type: tool
    platform: macOS
    sources:
      - path: Sources/CLI
    dependencies:
      - target: OpenFocusCore
      - target: OpenFocusData
      - target: OpenFocusCLIKit
    settings:
      base:
        PRODUCT_NAME: openfocus
        PRODUCT_BUNDLE_IDENTIFIER: dev.openfocus.cli

  OpenFocusCoreTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests/UnitTests
    dependencies:
      - target: OpenFocusCore
      - target: OpenFocusCLIKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.openfocus.core.tests
        GENERATE_INFOPLIST_FILE: YES

  OpenFocusDataTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests/DataTests
    dependencies:
      - target: OpenFocusCore
      - target: OpenFocusData
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.openfocus.data.tests
        GENERATE_INFOPLIST_FILE: YES

schemes:
  OpenFocus-macOS:
    build:
      targets:
        OpenFocus-macOS: all
        OpenFocusCoreTests: [test]
    run:
      config: Debug
    test:
      targets:
        - OpenFocusCoreTests
  OpenFocusDataTests:
    build:
      targets:
        OpenFocusDataTests: [test]
    test:
      targets:
        - OpenFocusDataTests
  OpenFocus-iOS:
    build:
      targets:
        OpenFocus-iOS: all
    run:
      config: Debug
    archive:
      config: Release
